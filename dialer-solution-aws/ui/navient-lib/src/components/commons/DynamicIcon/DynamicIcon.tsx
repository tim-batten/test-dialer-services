/* eslint-disable no-unused-vars */
// import { SvgIconProps } from '@material-ui/core';
import React from 'react';
// const upperFirst = (string: string) => {
//   return string.slice(0, 1).toUpperCase() + string.slice(1, string.length);
// };

// const fixIconNames = (string: string) => {
//   const name = string.split('_').map(upperFirst).join('');
//   if (name === '3dRotation') {
//     return 'ThreeDRotation';
//   } else if (name === '4k') {
//     return 'FourK';
//   } else if (name === '360') {
//     return 'ThreeSixty';
//   }
//   return name;
// };

// type Props = SvgIconProps & {
//   iconName: string;
// };

export const DynamicIcon: React.FunctionComponent = (props) => {
  // const _iconName = fixIconNames(props.iconName);
  // const Icon = require(`@material-ui/icons/${_iconName}`).default;
  // return <Icon {...props} />;
  return <div />;
};
